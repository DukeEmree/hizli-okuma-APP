EXPO_PUBLIC_CONVEX_URL=
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=
EXPO_PUBLIC_AMPLITUDE_API_KEY=
EXPO_PUBLIC_SENTRY_DSN=

# Convex deployment env vars (set via `npx convex env set`, not read by the client):
# REVENUECAT_WEBHOOK_AUTH_HEADER=  # must match the Authorization header value configured on the RevenueCat webhook
